# Mriia AI Tutor - Frontend

Сучасний React frontend для Mriia AI Tutor з інтерфейсом для всіх API endpoints.

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
cd frontend
npm install
```

### Запуск у режимі розробки

```bash
npm run dev
```

Frontend буде доступний на `http://localhost:3000`

### Білд для продакшена

```bash
npm run build
```

Скомпільовані файли будуть у директорії `dist/`

## 📁 Структура проєкту

```
frontend/
├── src/
│   ├── components/
│   │   ├── QueryForm.jsx        # Компонент для /tutor/query
│   │   ├── AnswerCheck.jsx      # Компонент для /tutor/check-answers
│   │   └── BenchmarkSolver.jsx  # Компонент для /benchmark/solve
│   ├── App.jsx                  # Головний компонент з routing
│   ├── App.css                  # Стилі для App
│   ├── main.jsx                 # Точка входу
│   └── index.css                # Глобальні стилі
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## 🎨 Особливості

- **Сучасний UI/UX** - Градієнти, анімації, адаптивний дизайн
- **React Router** - Навігація між сторінками
- **Axios** - HTTP запити до API
- **Vite** - Швидка розробка та білд
- **Українська локалізація** - Весь інтерфейс українською

## 🔌 API Endpoints

Frontend інтегрується з такими endpoints:

- `POST /tutor/query` - Генерація лекційного матеріалу та практичних питань
- `POST /tutor/check-answers` - Перевірка відповідей учня
- `POST /benchmark/solve` - Розв'язання benchmark питань

## ⚙️ Конфігурація

Для зміни URL API створіть файл `.env` у директорії `frontend/`:

```env
VITE_API_URL=http://localhost:8000
```

За замовчуванням використовується `http://localhost:8000`

## 📱 Адаптивність

Інтерфейс адаптивний та працює на:
- Desktop
- Tablet
- Mobile

## 🛠️ Технології

- React 18
- React Router 6
- Vite 5
- Axios
- CSS3 (Flexbox, Grid)
