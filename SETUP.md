# 🚀 Setup Guide - Mriia AI Tutor

Детальний покроковий гайд для налаштування проекту Mriia AI Tutor.

## 📋 Зміст

1. [Вимоги](#вимоги)
2. [Крок 1: Клонування та підготовка](#крок-1-клонування-та-підготовка)
3. [Крок 2: Налаштування змінних середовища](#крок-2-налаштування-змінних-середовища)
4. [Крок 3: Ініціалізація ChromaDB](#крок-3-ініціалізація-chromadb)
5. [Крок 4: Запуск додатку](#крок-4-запуск-додатку)
6. [Крок 5: Перевірка роботи](#крок-5-перевірка-роботи)
7. [Розробка локально (без Docker)](#розробка-локально-без-docker)
8. [Усунення проблем](#усунення-проблем)

---

## Вимоги

### Обов'язкові

- **Docker** версії 20.10+ та **Docker Compose** версії 2.0+
- **API ключ** для Lapathon LLM сервісу
- **Git** для клонування репозиторію

### Опціональні (для локальної розробки)

- **Python** 3.10 або новіший
- **Node.js** 18+ та **npm** 9+
- **Віртуальне середовище Python** (venv)

### Перевірка встановлення

```bash
# Перевірка Docker
docker --version
docker-compose --version

# Перевірка Python (якщо використовуєте локальну розробку)
python --version  # Має бути 3.10+

# Перевірка Node.js (для frontend)
node --version
npm --version
```

---

## Крок 1: Клонування та підготовка

### 1.1 Клонуйте репозиторій

```bash
git clone <repository-url>
cd Groke-Lapa
```

### 1.2 Перевірте структуру проекту

Переконайтеся, що у вас є такі файли та папки:

```
Groke-Lapa/
├── .env                    # Буде створено на наступному кроці
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── data/
│   ├── benchmark_scores.parquet
│   └── benchmark_absences.parquet
├── Lapathon2026_Mriia_public_files/
│   └── text-embedding-qwen/
│       ├── toc_for_hackathon_with_subtopics.parquet
│       └── pages_for_hackathon.parquet
└── src/
```

**Важливо:** Переконайтеся, що файли `.parquet` знаходяться в правильних місцях!

---

## Крок 2: Налаштування змінних середовища

### 2.1 Створіть файл `.env`

**Windows (PowerShell):**
```powershell
New-Item -Path .env -ItemType File
```

**Linux/Mac:**
```bash
touch .env
```

**Або вручну:** Створіть файл `.env` в кореневій директорії проекту.

### 2.2 Додайте API ключ

Відкрийте файл `.env` та додайте ваш API ключ:

```env
LAPATHON_API_KEY=your_actual_api_key_here
```

**⚠️ УВАГА:** 
- Замініть `your_actual_api_key_here` на ваш реальний API ключ
- Ніколи не комітьте файл `.env` в Git!
- Без цього ключа додаток не зможе генерувати контент

### 2.3 Опціональні змінні (якщо потрібно)

Ви можете додати інші змінні середовища:

```env
LAPATHON_API_KEY=your_api_key_here

# Опціонально - змініть тільки якщо потрібно
PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006/v1/traces
CHROMA_PERSIST_DIR=./data/chroma_db
DATA_DIR=./data
```

---

## Крок 3: Ініціалізація ChromaDB

ChromaDB - це векторна база даних, яка зберігає інформацію про теми та сторінки підручників. Її потрібно ініціалізувати **один раз** перед першим запуском.

### 3.1 Запустіть Docker контейнери

```bash
docker-compose up -d
```

Це запустить:
- API сервер (поки що не працює повноцінно)
- Phoenix UI для трейсингу

### 3.2 Ініціалізуйте TOC колекцію

TOC (Table of Contents) - це колекція тем з підручників для семантичного пошуку:

```bash
docker-compose exec api python scripts/setup/setup_chroma_toc.py
```

**Очікуваний результат:**
```
============================================================
ChromaDB TOC Topics Setup
============================================================
   Found data file at: /app/Lapathon2026_Mriia_public_files/...
1. Loading TOC data from: ...
   ✓ Loaded 237 TOC topics
...
✓ ChromaDB TOC collection setup complete!
Collection: 'toc_topics'
Documents: 237
```

### 3.3 Ініціалізуйте Pages колекцію

Pages - це колекція сторінок підручників для отримання контексту:

```bash
docker-compose exec api python scripts/setup/setup_chroma_pages.py
```

**Очікуваний результат:**
```
============================================================
ChromaDB Pages Setup
============================================================
   Found data file at: /app/Lapathon2026_Mriia_public_files/...
1. Loading pages data from: ...
   ✓ Loaded 1318 pages
...
✓ ChromaDB pages collection setup complete!
Collection: 'pages'
Documents: 1318
```

### 3.4 Перевірка успішності

Після успішного виконання обох скриптів ви повинні побачити:
- ✅ TOC collection: ~237 документів
- ✅ Pages collection: ~1318 документів

Дані зберігаються в `data/chroma_db/` (локально) або в Docker volume `chroma_data`.

**Примітка:** Якщо ви видалите папку `data/chroma_db/`, вам потрібно буде запустити setup скрипти знову.

---

## Крок 4: Запуск додатку

### Варіант A: Docker Compose (Рекомендовано)

#### 4.1 Перебудова та запуск

```bash
# Перебудова контейнерів (якщо змінювали код)
docker-compose up --build

# Або в фоновому режимі
docker-compose up --build -d
```

#### 4.2 Перевірка статусу

```bash
# Перевірка запущених контейнерів
docker-compose ps

# Перегляд логів
docker-compose logs -f api
```

#### 4.3 Доступні сервіси

Після запуску будуть доступні:

- **API сервер:** http://localhost:8000
  - Health check: http://localhost:8000/health
  - API документація: http://localhost:8000/docs
  
- **Phoenix UI:** http://localhost:6006
  - Для моніторингу та трейсингу LLM викликів

### Варіант B: Локальна розробка

#### 4.1 Backend (Термінал 1)

```bash
# Створіть віртуальне середовище (якщо ще не створено)
python -m venv venv

# Активуйте віртуальне середовище
# Windows PowerShell:
venv\Scripts\Activate.ps1
# Windows CMD:
venv\Scripts\activate.bat
# Linux/Mac:
source venv/bin/activate

# Встановіть залежності
pip install -r requirements.txt

# Запустіть FastAPI сервер
python -m src.main
```

API буде доступний на http://localhost:8000

#### 4.2 Frontend (Термінал 2)

```bash
cd frontend

# Встановіть залежності (перший раз)
npm install

# Запустіть dev сервер
npm run dev
```

Frontend буде доступний на http://localhost:5173 (або інший порт, якщо 5173 зайнятий)

---

## Крок 5: Перевірка роботи

### 5.1 Перевірка API Health

```bash
curl http://localhost:8000/health
```

**Очікувана відповідь:**
```json
{"status":"healthy","service":"mriia-tutor"}
```

### 5.2 Тестовий запит через API

```bash
curl -X POST http://localhost:8000/tutor/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Складні речення та їх ознаки",
    "grade": 9,
    "subject": "Українська мова"
  }'
```

**Очікувана відповідь:** JSON з полями:
- `lecture_content` - згенерований конспект
- `matched_topics` - знайдені теми
- `practice_questions` - практичні питання

### 5.3 Тест через Frontend

1. Відкрийте http://localhost:5173 (або інший порт з терміналу)
2. Заповніть форму:
   - **Запит:** "Складні речення та їх ознаки"
   - **Клас:** 9
   - **Предмет:** Українська мова
3. Натисніть "Надіслати запит"
4. Перевірте, що з'явився конспект та питання

### 5.4 Перевірка Phoenix Tracing

1. Відкрийте http://localhost:6006
2. Виконайте кілька запитів через API або frontend
3. Перевірте, що в Phoenix з'являються трейси з:
   - LLM викликами
   - Topic routing
   - Content generation

---

## Розробка локально (без Docker)

Якщо ви хочете розробляти без Docker:

### 1. Встановіть Python залежності

```bash
python -m venv venv
source venv/bin/activate  # або venv\Scripts\Activate.ps1 на Windows
pip install -r requirements.txt
```

### 2. Налаштуйте змінні середовища

Створіть `.env` файл (див. Крок 2)

### 3. Ініціалізуйте ChromaDB локально

```bash
# Переконайтеся, що віртуальне середовище активовано
python scripts/setup/setup_chroma_toc.py
python scripts/setup/setup_chroma_pages.py
```

### 4. Запустіть сервери

**Backend:**
```bash
python -m src.main
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

### 5. Налаштування шляхів

Якщо виникають проблеми з шляхами, перевірте:
- `src/config.py` - налаштування шляхів до даних
- `CHROMA_PERSIST_DIR` в `.env` - шлях до ChromaDB
- `DATA_DIR` в `.env` - шлях до parquet файлів

---

## Усунення проблем

### Проблема: "ModuleNotFoundError: No module named 'src'"

**Причина:** PYTHONPATH не налаштовано правильно.

**Рішення:**
1. Переконайтеся, що Dockerfile містить `ENV PYTHONPATH=/app`
2. Перебудова контейнера:
   ```bash
   docker-compose build api
   docker-compose up -d
   ```

### Проблема: "ChromaDB collection 'toc_topics' not found"

**Причина:** ChromaDB колекції не ініціалізовані.

**Рішення:**
```bash
docker-compose exec api python scripts/setup/setup_chroma_toc.py
docker-compose exec api python scripts/setup/setup_chroma_pages.py
```

### Проблема: "LAPATHON_API_KEY is not set"

**Причина:** Файл `.env` не створено або не містить API ключ.

**Рішення:**
1. Створіть `.env` файл в корені проекту
2. Додайте: `LAPATHON_API_KEY=your_key_here`
3. Перезапустіть контейнери: `docker-compose restart`

### Проблема: "Port 8000 is already allocated"

**Причина:** Порт 8000 вже використовується іншим процесом.

**Рішення:**

**Варіант 1:** Зупиніть інший процес
```bash
# Знайдіть процес, який використовує порт
# Windows:
netstat -ano | findstr :8000
# Linux/Mac:
lsof -i :8000

# Зупиніть процес або змініть порт в docker-compose.yml
```

**Варіант 2:** Змініть порт в `docker-compose.yml`
```yaml
ports:
  - "8001:8000"  # Змініть 8000 на 8001
```

### Проблема: "lecture_content is empty"

**Причина:** Можливі кілька причин.

**Рішення:**

1. **Перевірте логи:**
   ```bash
   docker-compose logs api | grep "Content Generator"
   ```

2. **Перевірте ChromaDB:**
   - Переконайтеся, що колекції ініціалізовані
   - Перевірте, що `matched_pages` не порожній в відповіді API

3. **Перевірте API ключ:**
   - Переконайтеся, що `LAPATHON_API_KEY` валідний
   - Перевірте логи на помилки LLM API

4. **Перевірте topic routing:**
   - Переконайтеся, що `matched_topics` не порожній
   - Спробуйте інший запит

### Проблема: "Parquet file not found"

**Причина:** Файли даних не знаходяться в правильному місці.

**Рішення:**
1. Переконайтеся, що файли є в:
   - `Lapathon2026_Mriia_public_files/text-embedding-qwen/toc_for_hackathon_with_subtopics.parquet`
   - `Lapathon2026_Mriia_public_files/text-embedding-qwen/pages_for_hackathon.parquet`

2. Перевірте, що Docker volume правильно підключений в `docker-compose.yml`

### Проблема: Frontend не підключається до API

**Причина:** Frontend намагається підключитися до неправильного URL.

**Рішення:**
1. Перевірте `frontend/.env` або `frontend/.env.local`
2. Додайте (якщо потрібно):
   ```env
   VITE_API_URL=http://localhost:8000
   ```
3. Перезапустіть frontend dev server

---

## Додаткові ресурси

- **API документація:** http://localhost:8000/docs (Swagger UI)
- **Phoenix Tracing:** http://localhost:6006
- **Основна документація:** [README.md](README.md)

---

## Підтримка

Якщо виникли проблеми, які не описані тут:

1. Перевірте логи: `docker-compose logs api`
2. Перевірте [README.md](README.md) секцію Troubleshooting
3. Створіть issue в репозиторії з деталями помилки

---

**Успішного налаштування! 🎉**
